/**
 * Stub for the `server-only` package.
 *
 * The real module throws when imported outside a React Server Component, which
 * would make every unit test of a server module fail. Vitest aliases this file
 * in its place (see vitest.config.ts).
 */
export {};
